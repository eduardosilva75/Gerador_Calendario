name: Build Windows Executable

on:
  push:
    tags: ['v*']  # Executa quando criar uma tag (ex: v1.0.0)
  release:
    types: [published]  # Executa quando criar um release
  workflow_dispatch:  # Permite execução manual

jobs:
  build-windows:
    name: Build Windows Executable
    runs-on: windows-latest
    
    steps:
    - name: Checkout código
      uses: actions/checkout@v4
      
    - name: Configurar Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        architecture: 'x64'
        
    - name: Instalar dependências
      run: |
        python -m pip install --upgrade pip
        pip install openpyxl altgraph pyinstaller
        
    - name: Verificar instalações
      run: |
        python --version
        pip list
        
    - name: Criar executável
      run: |
        pyinstaller --onefile --console --name "Gerador_Calendario_Folgas" geralClaude.py
        dir dist\
        
    - name: Upload executável como artefacto
      uses: actions/upload-artifact@v4
      with:
        name: Gerador-Calendario-Windows
        path: dist/Gerador_Calendario_Folgas.exe
        retention-days: 30
        
    - name: Criar Release (apenas para tags)
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      with:
        files: dist/Gerador_Calendario_Folgas.exe
        body: |
          # Gerador de Calendário 2026 com Folgas
          
          ## Funcionalidades:
          - 📅 Calendário completo de 2026
          - 🔄 Ciclo de folgas personalizável (1-12 semanas)
          - 🇵🇹 Feriados de Portugal incluídos
          - 🎨 Cores para melhor visualização
          - 📊 Exportação para Excel
          
          ## Como usar:
          1. Execute o programa
          2. Escolha a semana de início do ciclo (1-12)
          3. O calendário será gerado automaticamente
          
          **Legenda de cores:**
          - 🔵 Azul: Folgas do ciclo
          - 🔴 Rosa: Feriados
          - 🟡 Amarelo: Fins de semana
          - ⚪ Branco: Dias de trabalho
